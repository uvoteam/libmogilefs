libmogilefs is the thread-safe library to upload files to mogileFS and check if key exists

Build:(cmake, boost, curl are required)
cmake .
make


libmogilefs is distributed under GNU Public License version 2 or above.
